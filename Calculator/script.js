function Operate(l, operator, r) {
    if (operator === '+')
        return l + r;
    if (operator === '-')
        return l - r;
    if (operator === '*')
        return l * r;
    if (operator === '/')
        return l / r;
}

